<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>FEC_Is_My_Follow_Up__c</fullName>
    <description>My Follow-Up</description>
    <externalId>false</externalId>
    <formula>AND(
  OR(
    CONTAINS(FEC_Follow_Up_User__c, CASESAFEID($User.Id)),
    CONTAINS(FEC_Follow_Up_User__c, $User.Id)
  ),
  OR(
    AND(
      OR(ISPICKVAL(Status, "Open"), ISPICKVAL(Status, "Pending")),
      OR(
        OwnerId &lt;&gt; $User.Id,
        FEC_Assignment_Count__c &gt; 0
      )
    ),
    NOT(OR(ISPICKVAL(Status, "Open"), ISPICKVAL(Status, "Pending")))
  )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Is My Follow Up</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>