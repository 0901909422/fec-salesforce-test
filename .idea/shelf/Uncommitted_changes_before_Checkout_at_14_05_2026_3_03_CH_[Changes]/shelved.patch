Index: force-app/main/default/classes/FEC_HoldCaseController.cls
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>public with sharing class FEC_HoldCaseController {\n\n    // ─────────────────────────────────────────────────────\n    // DTO: HoldCaseModel\n    // ─────────────────────────────────────────────────────\n    public class HoldCaseModel {\n        @AuraEnabled public Id      caseId;\n        @AuraEnabled public String  nfuReason;\n        @AuraEnabled public String  nfuCode;\n        @AuraEnabled public Integer holdDuration;\n    }\n\n    public class NfuInfoResult {\n        @AuraEnabled public String nfuStatus;\n        @AuraEnabled public String nfuCode;\n        @AuraEnabled public String nfuStartedDate;\n        @AuraEnabled public String nfuExpiryDate;\n        @AuraEnabled public String nfuReason;\n        @AuraEnabled public Boolean isSuccess;\n    }\n\n    // ─────────────────────────────────────────────────────\n    // DTO: ResponseModel\n    // ─────────────────────────────────────────────────────\n    public class ResponseModel {\n        @AuraEnabled public String  status;\n        @AuraEnabled public String  message;\n        @AuraEnabled public Id      recordId;\n        @AuraEnabled public Boolean isSuccess;\n\n        public ResponseModel success(Id recId, String msg) {\n            this.status    = FEC_ConstantCommon.STATUS_SUCCESS;\n            this.message   = msg;\n            this.recordId  = recId;\n            this.isSuccess = true;\n            return this;\n        }\n\n        public ResponseModel error(String msg) {\n            this.status    = FEC_ConstantCommon.CASE_STATUS_ERROR;\n            this.message   = msg;\n            this.isSuccess = false;\n            return this;\n        }\n    }\n\n    // ─────────────────────────────────────────────────────\n    // DTO: NFUReasonModel\n    // ─────────────────────────────────────────────────────\n    public class NFUReasonModel {\n        @AuraEnabled public String  nfuCode;\n        @AuraEnabled public Decimal nfuDuration;\n        @AuraEnabled public String  nfuReasonVN;\n        @AuraEnabled public String  nfuReasonEN;\n        @AuraEnabled public Boolean isActive;\n        @AuraEnabled public String  holdCaseType;\n        @AuraEnabled public String  subCode;\n\n        public NFUReasonModel() {}\n    }\n\n    // ─────────────────────────────────────────────────────\n    // METHOD: getNFUReasons\n    // ─────────────────────────────────────────────────────\n    @AuraEnabled(cacheable=true)\n    public static List<NFUReasonModel> getNFUReasons(String subCode) {\n\n        if (String.isBlank(subCode)) {\n            throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'subCode'}));\n        }\n\n        try {\n            // 1. Query Config → lấy NFU Codes + metadata\n            List<FEC_Hold_Case_Config__c> configs = [\n                SELECT FEC_NFU_Code__c,\n                       FEC_Active__c,\n                       Hold_Case_Type__c\t\n                FROM FEC_Hold_Case_Config__c\n                WHERE FEC_Nature_of_Cases__r.FEC_Sub_Code__c = :subCode\n                AND   FEC_Active__c   = true\n                WITH SECURITY_ENFORCED\n            ];\n\n            if (configs.isEmpty()) {\n                return new List<NFUReasonModel>();\n            }\n\n            Map<String, FEC_Hold_Case_Config__c> configMap = new Map<String, FEC_Hold_Case_Config__c>();\n            Set<String> nfuCodes = new Set<String>();\n\n            for (FEC_Hold_Case_Config__c cfg : configs) {\n                if (String.isBlank(cfg.FEC_NFU_Code__c)) continue;\n\n                for (String code : cfg.FEC_NFU_Code__c.split(',')) {\n                    String trimmed = code.trim();\n                    if (!String.isBlank(trimmed)) {\n                        nfuCodes.add(trimmed);\n                        configMap.put(trimmed, cfg);\n                    }\n                }\n            }\n\n            if (nfuCodes.isEmpty()) {\n                return new List<NFUReasonModel>();\n            }\n\n            List<FEC_Hold_Case__c> holdCases = [\n                SELECT FEC_NFU_Code__c,\n                       FEC_NFU_Reason_VN__c,\n                       FEC_NFU_Reason_EN__c,\n                       FEC_NFU_Duration__c\n                FROM FEC_Hold_Case__c\n                WHERE FEC_NFU_Code__c IN :nfuCodes\n                WITH SECURITY_ENFORCED\n            ];\n\n            if (holdCases.isEmpty()) {\n                return new List<NFUReasonModel>();\n            }\n\n            List<NFUReasonModel> result = new List<NFUReasonModel>();\n\n            for (FEC_Hold_Case__c hc : holdCases) {\n                FEC_Hold_Case_Config__c cfg = configMap.get(hc.FEC_NFU_Code__c);\n\n                NFUReasonModel m  = new NFUReasonModel();\n                m.nfuCode         = hc.FEC_NFU_Code__c;\n                m.nfuDuration     = hc.FEC_NFU_Duration__c;\n                m.nfuReasonVN     = hc.FEC_NFU_Reason_VN__c;\n                m.nfuReasonEN     = hc.FEC_NFU_Reason_EN__c;\n                m.isActive        = cfg != null ? cfg.FEC_Active__c         : false;\n                m.holdCaseType    = cfg != null ? cfg.Hold_Case_Type__c\t : '';\n                m.subCode         = subCode;\n\n                result.add(m);\n            }\n\n            return result;\n\n        } catch (Exception e) {\n            throw new AuraHandledException('Error: ' + e.getMessage());\n        }\n    }\n\n    /**\n     * Gọi API Mark NFU (FEC_MarkNFU) rồi trả về NFU info từ Case.\n     * Non-cacheable vì có HTTP callout.\n     * Dùng khi Routing Action = \"Resolve\" | \"Route to\".\n     */\n    @AuraEnabled\n    public static NfuInfoResult callMarkNFUAndGetInfo(String caseId) {\n        if (String.isBlank(caseId)) {\n            throw new AuraHandledException(\n                String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'caseId'})\n            );\n        }\n\n        try {\n            // 1. Query Case lấy ContractNumber\n            Case c = [\n                SELECT Id,\n                    FEC_Account_Contract_Number_Text__c,\n                    FEC_SubCode__c,\n                    FEC_NFU_Status__c,\n                    FEC_NFU_Started_Date__c,\n                    FEC_NFU_Expiry_Date__c,\n                    FEC_NFU_Reason__c\n                FROM Case\n                WHERE Id = :caseId\n                WITH SECURITY_ENFORCED\n                LIMIT 1\n            ];\n            System.debug('[callMarkNFUAndGetInfo] Case: ' + JSON.serialize(c));\n            // 2. Lấy NFU Code từ Config theo subCode\n            List<FEC_Hold_Case_Config__c> configs = [\n                SELECT FEC_NFU_Code__c\n                FROM FEC_Hold_Case_Config__c\n                WHERE FEC_Nature_of_Cases__r.FEC_Sub_Code__c = :c.FEC_SubCode__c\n                AND   FEC_Active__c   = true\n                WITH SECURITY_ENFORCED\n                LIMIT 1\n            ];\n             System.debug('[callMarkNFUAndGetInfo] configs: ' + JSON.serialize(configs));\n            if (configs.isEmpty() || String.isBlank(configs[0].FEC_NFU_Code__c)) {\n                NfuInfoResult failed = new NfuInfoResult();\n                failed.isSuccess = false;\n                failed.nfuReason = System.Label.FEC_Reason_Error;\n                return failed;\n            }\n\n            String nfuCode = configs[0].FEC_NFU_Code__c.split(',')[0].trim();\n\n             System.debug('[callMarkNFUAndGetInfo] nfuCode to send: ' + nfuCode);\n\n            // 2. Gọi API Mark NFU\n            String userEmail = [\n                SELECT Email FROM User\n                WHERE Id = :UserInfo.getUserId()\n                LIMIT 1\n            ].Email;\n           \n            FEC_MarkNFU.MarkNFURequest req = new FEC_MarkNFU.MarkNFURequest();\n            req.ContractNumber = c.FEC_Account_Contract_Number_Text__c;\n            req.NFUReasonCode  = nfuCode;\n            req.NFUUpdatedBy   = userEmail;\n            System.debug('[callMarkNFUAndGetInfo] MarkNFURequest: ' + JSON.serialize(req));\n\n            FEC_MarkNFU.MarkNFUResult markResult = FEC_MarkNFU.markNFU(req);\n            System.debug('[callMarkNFUAndGetInfo] MarkNFUResult: ' + JSON.serialize(markResult));\n            NfuInfoResult result = new NfuInfoResult();\n\n            if (!markResult.success) {\n                result.isSuccess = false;\n                result.nfuReason = markResult.errorMessage;\n                return result;\n            }\n\n            Case refreshed = [\n                SELECT FEC_NFU_Status__c,\n                    FEC_NFU_Code__c,\n                    FEC_NFU_Started_Date__c,\n                    FEC_NFU_Expiry_Date__c,\n                    FEC_NFU_Reason__c\n                FROM Case\n                WHERE Id = :caseId\n                LIMIT 1\n            ];\n        \n            if (String.isBlank(refreshed.FEC_NFU_Status__c)) {\n                result.isSuccess = false;\n                return result;\n            }\n\n            result.isSuccess      = true;\n            result.nfuStatus      = refreshed.FEC_NFU_Status__c;\n            result.nfuCode        = refreshed.FEC_NFU_Code__c;\n            result.nfuStartedDate = refreshed.FEC_NFU_Started_Date__c != null\n                ? refreshed.FEC_NFU_Started_Date__c.format()\n                : '';\n            result.nfuExpiryDate  = refreshed.FEC_NFU_Expiry_Date__c != null\n                ? refreshed.FEC_NFU_Expiry_Date__c.format()\n                : '';\n            result.nfuReason      = refreshed.FEC_NFU_Reason__c;\n\n            return result;\n\n        } catch (Exception e) {\n            throw new AuraHandledException('callMarkNFUAndGetInfo error: ' + e.getMessage());\n        }\n    }\n\n    // ─────────────────────────────────────────────────────\n    // METHOD: saveHoldCase\n    // ─────────────────────────────────────────────────────\n    @AuraEnabled\n    public static ResponseModel saveHoldCase(\n        Id      caseId,\n        String  nfuReason,\n        String  nfuCode,\n        Integer holdDuration\n    ) {\n        HoldCaseModel model = new HoldCaseModel();\n        model.caseId       = caseId;\n        model.nfuReason    = nfuReason;\n        model.nfuCode      = nfuCode;\n        model.holdDuration = holdDuration;\n\n        validateModel(model);\n\n        Savepoint sp = Database.setSavepoint();\n\n        try {\n            Case c = [\n                SELECT Id, Status\n                FROM Case\n                WHERE Id = :model.caseId\n                LIMIT 1\n            ];\n\n            c.Status = FEC_ConstantCommon.CASE_STATUS_ON_HOLD;\n            update c;\n\n            return new ResponseModel().success(c.Id, System.Label.FEC_Succes_Hold_Case);\n\n        } catch (Exception e) {\n            Database.rollback(sp);\n            return new ResponseModel().error(e.getMessage());\n        }\n    }\n\n    // ─────────────────────────────────────────────────────\n    // HELPER: check duplicate theo caseId + nfuCode\n    // ─────────────────────────────────────────────────────\n    private static Boolean isNFUCodeExist(String nfuCode, Id caseId) {\n        return ![\n            SELECT Id\n            FROM FEC_Hold_Case__c\n            WHERE FEC_NFU_Code__c = :nfuCode\n            LIMIT 1\n        ].isEmpty();\n    }\n\n    // ─────────────────────────────────────────────────────\n    // HELPER: validate model\n    // ─────────────────────────────────────────────────────\n    @TestVisible\n    private static void validateModel(HoldCaseModel model) {\n        if (model == null) {\n            throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'Model'}));\n        }\n        if (model.caseId == null) {\n            throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'Case Id'}));\n        }\n        if (String.isBlank(model.nfuReason)) {\n            throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'NFU Reason'}));\n        }\n        if (String.isBlank(model.nfuCode)) {\n            throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'NFU Code'}));\n        }\n    }\n}
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/force-app/main/default/classes/FEC_HoldCaseController.cls b/force-app/main/default/classes/FEC_HoldCaseController.cls
--- a/force-app/main/default/classes/FEC_HoldCaseController.cls	(revision 31f8a0255be6d9716986d53078c581c26c26130c)
+++ b/force-app/main/default/classes/FEC_HoldCaseController.cls	(date 1778742324262)
@@ -27,19 +27,38 @@
         @AuraEnabled public String  message;
         @AuraEnabled public Id      recordId;
         @AuraEnabled public Boolean isSuccess;
+        // tungnm37: phân biệt TH1 (already marked) vs TH2 (success)
+        @AuraEnabled public String  responseType; // 'ALREADY_MARKED' | 'SUCCESS' | 'ERROR'
+        // tungnm37: NFU info sau khi gọi API thành công
+        @AuraEnabled public String  nfuCode;
+        @AuraEnabled public String  nfuStatus;
+        @AuraEnabled public String  nfuStartedDate;
+        @AuraEnabled public String  nfuExpiryDate;
+        @AuraEnabled public String  nfuReason;
 
         public ResponseModel success(Id recId, String msg) {
-            this.status    = FEC_ConstantCommon.STATUS_SUCCESS;
-            this.message   = msg;
-            this.recordId  = recId;
-            this.isSuccess = true;
+            this.status       = FEC_ConstantCommon.STATUS_SUCCESS;
+            this.message      = msg;
+            this.recordId     = recId;
+            this.isSuccess    = true;
+            this.responseType = 'SUCCESS';
+            return this;
+        }
+
+        public ResponseModel alreadyMarked(Id recId, String msg) {
+            this.status       = FEC_ConstantCommon.STATUS_SUCCESS;
+            this.message      = msg;
+            this.recordId     = recId;
+            this.isSuccess    = true;
+            this.responseType = 'ALREADY_MARKED';
             return this;
         }
 
         public ResponseModel error(String msg) {
-            this.status    = FEC_ConstantCommon.CASE_STATUS_ERROR;
-            this.message   = msg;
-            this.isSuccess = false;
+            this.status       = FEC_ConstantCommon.CASE_STATUS_ERROR;
+            this.message      = msg;
+            this.isSuccess    = false;
+            this.responseType = 'ERROR';
             return this;
         }
     }
@@ -61,36 +80,59 @@
 
     // ─────────────────────────────────────────────────────
     // METHOD: getNFUReasons
+    // tungnm37 fix: query theo NOC (caseId) thay vì subCode text
     // ─────────────────────────────────────────────────────
     @AuraEnabled(cacheable=true)
-    public static List<NFUReasonModel> getNFUReasons(String subCode) {
+    public static List<NFUReasonModel> getNFUReasons(String caseId) {
 
-        if (String.isBlank(subCode)) {
-            throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'subCode'}));
+        if (String.isBlank(caseId)) {
+            throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'caseId'}));
         }
 
         try {
-            // 1. Query Config → lấy NFU Codes + metadata
+            // tungnm37: lấy NOC từ Case (Product Type + Category + SubCategory + SubCode)
+            List<Case> cases = [
+                SELECT FEC_Product_Type__c, FEC_Category__c, FEC_SubCategory__c, FEC_SubCode__c
+                FROM Case
+                WHERE Id = :caseId
+                WITH SECURITY_ENFORCED
+                LIMIT 1
+            ];
+            if (cases.isEmpty()) return new List<NFUReasonModel>();
+
+            Case c = cases[0];
+
+            // tungnm37: tìm NOC khớp với Case
+            List<FEC_Nature_of_Case__c> nocs = [
+                SELECT Id
+                FROM FEC_Nature_of_Case__c
+                WHERE FEC_Product_Type__c = :c.FEC_Product_Type__c
+                  AND FEC_Category__c     = :c.FEC_Category__c
+                  AND FEC_Sub_Category__c = :c.FEC_SubCategory__c
+                  AND FEC_Sub_Code__c     = :c.FEC_SubCode__c
+                WITH SECURITY_ENFORCED
+                LIMIT 1
+            ];
+            if (nocs.isEmpty()) return new List<NFUReasonModel>();
+
+            Id nocId = nocs[0].Id;
+
+            // tungnm37: query Hold Case Config theo NOC Id
             List<FEC_Hold_Case_Config__c> configs = [
-                SELECT FEC_NFU_Code__c,
-                       FEC_Active__c,
-                       Hold_Case_Type__c	
+                SELECT FEC_NFU_Code__c, FEC_Active__c, Hold_Case_Type__c
                 FROM FEC_Hold_Case_Config__c
-                WHERE FEC_Nature_of_Cases__r.FEC_Sub_Code__c = :subCode
-                AND   FEC_Active__c   = true
+                WHERE FEC_Nature_of_Cases__c = :nocId
+                  AND FEC_Active__c = true
                 WITH SECURITY_ENFORCED
             ];
 
-            if (configs.isEmpty()) {
-                return new List<NFUReasonModel>();
-            }
+            if (configs.isEmpty()) return new List<NFUReasonModel>();
 
             Map<String, FEC_Hold_Case_Config__c> configMap = new Map<String, FEC_Hold_Case_Config__c>();
             Set<String> nfuCodes = new Set<String>();
 
             for (FEC_Hold_Case_Config__c cfg : configs) {
                 if (String.isBlank(cfg.FEC_NFU_Code__c)) continue;
-
                 for (String code : cfg.FEC_NFU_Code__c.split(',')) {
                     String trimmed = code.trim();
                     if (!String.isBlank(trimmed)) {
@@ -100,41 +142,29 @@
                 }
             }
 
-            if (nfuCodes.isEmpty()) {
-                return new List<NFUReasonModel>();
-            }
+            if (nfuCodes.isEmpty()) return new List<NFUReasonModel>();
 
             List<FEC_Hold_Case__c> holdCases = [
-                SELECT FEC_NFU_Code__c,
-                       FEC_NFU_Reason_VN__c,
-                       FEC_NFU_Reason_EN__c,
-                       FEC_NFU_Duration__c
+                SELECT FEC_NFU_Code__c, FEC_NFU_Reason_VN__c, FEC_NFU_Reason_EN__c, FEC_NFU_Duration__c
                 FROM FEC_Hold_Case__c
                 WHERE FEC_NFU_Code__c IN :nfuCodes
                 WITH SECURITY_ENFORCED
             ];
 
-            if (holdCases.isEmpty()) {
-                return new List<NFUReasonModel>();
-            }
+            if (holdCases.isEmpty()) return new List<NFUReasonModel>();
 
             List<NFUReasonModel> result = new List<NFUReasonModel>();
-
             for (FEC_Hold_Case__c hc : holdCases) {
                 FEC_Hold_Case_Config__c cfg = configMap.get(hc.FEC_NFU_Code__c);
-
-                NFUReasonModel m  = new NFUReasonModel();
-                m.nfuCode         = hc.FEC_NFU_Code__c;
-                m.nfuDuration     = hc.FEC_NFU_Duration__c;
-                m.nfuReasonVN     = hc.FEC_NFU_Reason_VN__c;
-                m.nfuReasonEN     = hc.FEC_NFU_Reason_EN__c;
-                m.isActive        = cfg != null ? cfg.FEC_Active__c         : false;
-                m.holdCaseType    = cfg != null ? cfg.Hold_Case_Type__c	 : '';
-                m.subCode         = subCode;
-
+                NFUReasonModel m = new NFUReasonModel();
+                m.nfuCode      = hc.FEC_NFU_Code__c;
+                m.nfuDuration  = hc.FEC_NFU_Duration__c;
+                m.nfuReasonVN  = hc.FEC_NFU_Reason_VN__c;
+                m.nfuReasonEN  = hc.FEC_NFU_Reason_EN__c;
+                m.isActive     = cfg != null ? cfg.FEC_Active__c : false;
+                m.holdCaseType = cfg != null ? cfg.Hold_Case_Type__c : '';
                 result.add(m);
             }
-
             return result;
 
         } catch (Exception e) {
@@ -142,10 +172,98 @@
         }
     }
 
+    // ─────────────────────────────────────────────────────
+    // METHOD: saveHoldCase
+    // tungnm37: gọi API Mark NFU + mapping response fields lên Case
+    // ─────────────────────────────────────────────────────
+    @AuraEnabled
+    public static ResponseModel saveHoldCase(
+        Id      caseId,
+        String  nfuReason,
+        String  nfuCode,
+        Integer holdDuration
+    ) {
+        HoldCaseModel model = new HoldCaseModel();
+        model.caseId       = caseId;
+        model.nfuReason    = nfuReason;
+        model.nfuCode      = nfuCode;
+        model.holdDuration = holdDuration;
+
+        validateModel(model);
+
+        Savepoint sp = Database.setSavepoint();
+
+        try {
+            // tungnm37: lấy Case info để gọi API
+            Case c = [
+                SELECT Id, Status, FEC_Account_Contract_Number_Text__c,
+                       FEC_NFU_Code__c, FEC_NFU_Status__c, FEC_NFU_Started_Date__c,
+                       FEC_NFU_Expiry_Date__c, FEC_NFU_Reason__c
+                FROM Case
+                WHERE Id = :model.caseId
+                LIMIT 1
+            ];
+
+            // tungnm37: lấy email user hiện tại
+            String userEmail = [SELECT Email FROM User WHERE Id = :UserInfo.getUserId() LIMIT 1].Email;
+
+            // tungnm37: gọi API Mark NFU
+            FEC_MarkNFU.MarkNFURequest req = new FEC_MarkNFU.MarkNFURequest();
+            req.ContractNumber = c.FEC_Account_Contract_Number_Text__c;
+            req.NFUReasonCode  = model.nfuCode;
+            req.NFUUpdatedBy   = userEmail;
+
+            FEC_MarkNFU.MarkNFUResult markResult = FEC_MarkNFU.markNFU(req);
+
+            ResponseModel resp = new ResponseModel();
+
+            if (markResult.success) {
+                // tungnm37: TH2 - Success → update Case fields + status On Hold
+                c.Status              = FEC_ConstantCommon.CASE_STATUS_ON_HOLD;
+                c.FEC_NFU_Code__c     = model.nfuCode;
+                c.FEC_NFU_Status__c   = 'Yes';
+                c.FEC_NFU_Reason__c   = model.nfuReason;
+                // FEC_NFU_Started_Date__c = NULL per spec
+                c.FEC_NFU_Started_Date__c = null;
+                // FEC_NFU_Expiry_Date__c = NFUTillDate từ response
+                if (String.isNotBlank(markResult.nfuTillDate)) {
+                    try {
+                        c.FEC_NFU_Expiry_Date__c = Date.valueOf(markResult.nfuTillDate.left(10));
+                    } catch (Exception ex) {
+                        c.FEC_NFU_Expiry_Date__c = null;
+                    }
+                }
+                update c;
+
+                resp.success(c.Id, System.Label.FEC_Succes_Hold_Case);
+                resp.nfuCode        = c.FEC_NFU_Code__c;
+                resp.nfuStatus      = c.FEC_NFU_Status__c;
+                resp.nfuReason      = c.FEC_NFU_Reason__c;
+                resp.nfuExpiryDate  = c.FEC_NFU_Expiry_Date__c != null ? c.FEC_NFU_Expiry_Date__c.format() : '';
+                return resp;
+
+            } else {
+                // tungnm37: TH1 - Contract has already marked as NFU
+                String apiDescription = markResult.description != null ? markResult.description : '';
+                if (apiDescription.containsIgnoreCase('already marked') || apiDescription.containsIgnoreCase('already been marked')) {
+                    // Vẫn update Case status On Hold
+                    c.Status = FEC_ConstantCommon.CASE_STATUS_ON_HOLD;
+                    update c;
+                    return resp.alreadyMarked(c.Id, apiDescription);
+                }
+                // tungnm37: lỗi khác → rollback
+                Database.rollback(sp);
+                return resp.error(markResult.errorMessage != null ? markResult.errorMessage : apiDescription);
+            }
+
+        } catch (Exception e) {
+            Database.rollback(sp);
+            return new ResponseModel().error(e.getMessage());
+        }
+    }
+
     /**
      * Gọi API Mark NFU (FEC_MarkNFU) rồi trả về NFU info từ Case.
-     * Non-cacheable vì có HTTP callout.
-     * Dùng khi Routing Action = "Resolve" | "Route to".
      */
     @AuraEnabled
     public static NfuInfoResult callMarkNFUAndGetInfo(String caseId) {
@@ -154,33 +272,31 @@
                 String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'caseId'})
             );
         }
-
         try {
-            // 1. Query Case lấy ContractNumber
             Case c = [
-                SELECT Id,
-                    FEC_Account_Contract_Number_Text__c,
-                    FEC_SubCode__c,
-                    FEC_NFU_Status__c,
-                    FEC_NFU_Started_Date__c,
-                    FEC_NFU_Expiry_Date__c,
-                    FEC_NFU_Reason__c
-                FROM Case
-                WHERE Id = :caseId
-                WITH SECURITY_ENFORCED
-                LIMIT 1
+                SELECT Id, FEC_Account_Contract_Number_Text__c, FEC_SubCode__c,
+                       FEC_NFU_Status__c, FEC_NFU_Started_Date__c, FEC_NFU_Expiry_Date__c, FEC_NFU_Reason__c
+                FROM Case WHERE Id = :caseId WITH SECURITY_ENFORCED LIMIT 1
             ];
-            System.debug('[callMarkNFUAndGetInfo] Case: ' + JSON.serialize(c));
-            // 2. Lấy NFU Code từ Config theo subCode
+
+            // tungnm37: query NOC theo Case fields
+            List<FEC_Nature_of_Case__c> nocs = [
+                SELECT Id FROM FEC_Nature_of_Case__c
+                WHERE FEC_Sub_Code__c = :c.FEC_SubCode__c
+                WITH SECURITY_ENFORCED LIMIT 1
+            ];
+            if (nocs.isEmpty()) {
+                NfuInfoResult failed = new NfuInfoResult();
+                failed.isSuccess = false;
+                failed.nfuReason = System.Label.FEC_Reason_Error;
+                return failed;
+            }
+
             List<FEC_Hold_Case_Config__c> configs = [
-                SELECT FEC_NFU_Code__c
-                FROM FEC_Hold_Case_Config__c
-                WHERE FEC_Nature_of_Cases__r.FEC_Sub_Code__c = :c.FEC_SubCode__c
-                AND   FEC_Active__c   = true
-                WITH SECURITY_ENFORCED
-                LIMIT 1
+                SELECT FEC_NFU_Code__c FROM FEC_Hold_Case_Config__c
+                WHERE FEC_Nature_of_Cases__c = :nocs[0].Id AND FEC_Active__c = true
+                WITH SECURITY_ENFORCED LIMIT 1
             ];
-             System.debug('[callMarkNFUAndGetInfo] configs: ' + JSON.serialize(configs));
             if (configs.isEmpty() || String.isBlank(configs[0].FEC_NFU_Code__c)) {
                 NfuInfoResult failed = new NfuInfoResult();
                 failed.isSuccess = false;
@@ -189,24 +305,14 @@
             }
 
             String nfuCode = configs[0].FEC_NFU_Code__c.split(',')[0].trim();
+            String userEmail = [SELECT Email FROM User WHERE Id = :UserInfo.getUserId() LIMIT 1].Email;
 
-             System.debug('[callMarkNFUAndGetInfo] nfuCode to send: ' + nfuCode);
-
-            // 2. Gọi API Mark NFU
-            String userEmail = [
-                SELECT Email FROM User
-                WHERE Id = :UserInfo.getUserId()
-                LIMIT 1
-            ].Email;
-           
             FEC_MarkNFU.MarkNFURequest req = new FEC_MarkNFU.MarkNFURequest();
             req.ContractNumber = c.FEC_Account_Contract_Number_Text__c;
             req.NFUReasonCode  = nfuCode;
             req.NFUUpdatedBy   = userEmail;
-            System.debug('[callMarkNFUAndGetInfo] MarkNFURequest: ' + JSON.serialize(req));
 
             FEC_MarkNFU.MarkNFUResult markResult = FEC_MarkNFU.markNFU(req);
-            System.debug('[callMarkNFUAndGetInfo] MarkNFUResult: ' + JSON.serialize(markResult));
             NfuInfoResult result = new NfuInfoResult();
 
             if (!markResult.success) {
@@ -216,32 +322,17 @@
             }
 
             Case refreshed = [
-                SELECT FEC_NFU_Status__c,
-                    FEC_NFU_Code__c,
-                    FEC_NFU_Started_Date__c,
-                    FEC_NFU_Expiry_Date__c,
-                    FEC_NFU_Reason__c
-                FROM Case
-                WHERE Id = :caseId
-                LIMIT 1
+                SELECT FEC_NFU_Status__c, FEC_NFU_Code__c, FEC_NFU_Started_Date__c,
+                       FEC_NFU_Expiry_Date__c, FEC_NFU_Reason__c
+                FROM Case WHERE Id = :caseId LIMIT 1
             ];
-        
-            if (String.isBlank(refreshed.FEC_NFU_Status__c)) {
-                result.isSuccess = false;
-                return result;
-            }
 
             result.isSuccess      = true;
             result.nfuStatus      = refreshed.FEC_NFU_Status__c;
             result.nfuCode        = refreshed.FEC_NFU_Code__c;
-            result.nfuStartedDate = refreshed.FEC_NFU_Started_Date__c != null
-                ? refreshed.FEC_NFU_Started_Date__c.format()
-                : '';
-            result.nfuExpiryDate  = refreshed.FEC_NFU_Expiry_Date__c != null
-                ? refreshed.FEC_NFU_Expiry_Date__c.format()
-                : '';
+            result.nfuStartedDate = refreshed.FEC_NFU_Started_Date__c != null ? refreshed.FEC_NFU_Started_Date__c.format() : '';
+            result.nfuExpiryDate  = refreshed.FEC_NFU_Expiry_Date__c != null ? refreshed.FEC_NFU_Expiry_Date__c.format() : '';
             result.nfuReason      = refreshed.FEC_NFU_Reason__c;
-
             return result;
 
         } catch (Exception e) {
@@ -249,73 +340,11 @@
         }
     }
 
-    // ─────────────────────────────────────────────────────
-    // METHOD: saveHoldCase
-    // ─────────────────────────────────────────────────────
-    @AuraEnabled
-    public static ResponseModel saveHoldCase(
-        Id      caseId,
-        String  nfuReason,
-        String  nfuCode,
-        Integer holdDuration
-    ) {
-        HoldCaseModel model = new HoldCaseModel();
-        model.caseId       = caseId;
-        model.nfuReason    = nfuReason;
-        model.nfuCode      = nfuCode;
-        model.holdDuration = holdDuration;
-
-        validateModel(model);
-
-        Savepoint sp = Database.setSavepoint();
-
-        try {
-            Case c = [
-                SELECT Id, Status
-                FROM Case
-                WHERE Id = :model.caseId
-                LIMIT 1
-            ];
-
-            c.Status = FEC_ConstantCommon.CASE_STATUS_ON_HOLD;
-            update c;
-
-            return new ResponseModel().success(c.Id, System.Label.FEC_Succes_Hold_Case);
-
-        } catch (Exception e) {
-            Database.rollback(sp);
-            return new ResponseModel().error(e.getMessage());
-        }
-    }
-
-    // ─────────────────────────────────────────────────────
-    // HELPER: check duplicate theo caseId + nfuCode
-    // ─────────────────────────────────────────────────────
-    private static Boolean isNFUCodeExist(String nfuCode, Id caseId) {
-        return ![
-            SELECT Id
-            FROM FEC_Hold_Case__c
-            WHERE FEC_NFU_Code__c = :nfuCode
-            LIMIT 1
-        ].isEmpty();
-    }
-
-    // ─────────────────────────────────────────────────────
-    // HELPER: validate model
-    // ─────────────────────────────────────────────────────
     @TestVisible
     private static void validateModel(HoldCaseModel model) {
-        if (model == null) {
-            throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'Model'}));
-        }
-        if (model.caseId == null) {
-            throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'Case Id'}));
-        }
-        if (String.isBlank(model.nfuReason)) {
-            throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'NFU Reason'}));
-        }
-        if (String.isBlank(model.nfuCode)) {
-            throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'NFU Code'}));
-        }
-    }
-}
\ No newline at end of file
+        if (model == null) throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'Model'}));
+        if (model.caseId == null) throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'Case Id'}));
+        if (String.isBlank(model.nfuReason)) throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'NFU Reason'}));
+        if (String.isBlank(model.nfuCode)) throw new AuraHandledException(String.format(System.Label.FEC_MSG_Param_Required, new List<String>{'NFU Code'}));
+    }
+}
Index: force-app/main/default/classes/FEC_SubProcessService.cls
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>public with sharing class FEC_SubProcessService {\n\n    public class SubProcessWrapper {\n\n        @AuraEnabled\n        public Boolean showHoldCase = false;\n\n        @AuraEnabled\n        public Boolean showRemovePhone = false;\n\n        @AuraEnabled\n        public Boolean showDNB = false;\n\n        @AuraEnabled\n        public Boolean showTransferCall = false;\n    }\n\n    @AuraEnabled(cacheable=true)\n    public static SubProcessWrapper getSubProcesses(\n        Id recordId,\n        Id productTypeId,\n        Id categoryId,\n        Id subCategoryId,\n        Id subCodeId\n    ) {\n\n        SubProcessWrapper result =\n            new SubProcessWrapper();\n\n        try {\n\n            // ===== VALIDATION =====\n\n            if (\n                productTypeId == null ||\n                categoryId == null ||\n                subCategoryId == null\n            ) {\n                return result;\n            }\n\n            // ===== BUILD QUERY =====\n\n            String query =\n                'SELECT ' +\n                    'Id, ' +\n                    'FEC_Remove_Phone__c, ' +\n                    'FEC_Do_Not_Bother__c, ' +\n                    'FEC_Transfer_Call_to_Collection__c ' +\n                'FROM FEC_Nature_of_Case__c ' +\n                'WHERE ' +\n                    'FEC_Product_Type__c = :productTypeId ' +\n                    'AND FEC_Category__c = :categoryId ' +\n                    'AND FEC_Sub_Category__c = :subCategoryId ';\n\n            // ===== SUB CODE =====\n\n            if (subCodeId == null) {\n\n                query +=\n                    'AND FEC_Sub_Code__c = null ';\n\n            } else {\n\n                query +=\n                    'AND FEC_Sub_Code__c = :subCodeId ';\n            }\n\n            query += 'LIMIT 1';\n\n            // ===== QUERY NOC =====\n\n            List<FEC_Nature_of_Case__c> nocList =\n                Database.query(query);\n\n            if (nocList.isEmpty()) {\n                return result;\n            }\n\n            FEC_Nature_of_Case__c noc =\n                nocList[0];\n\n            // ===== REMOVE PHONE =====\n\n            result.showRemovePhone =\n                noc.FEC_Remove_Phone__c;\n\n            // ===== DNB =====\n\n            result.showDNB =\n                noc.FEC_Do_Not_Bother__c;\n\n            // ===== TRANSFER CALL =====\n\n            result.showTransferCall =\n                noc.FEC_Transfer_Call_to_Collection__c;\n\n            // ===== HOLD CASE =====\n\n            List<FEC_Hold_Case_Config__c> holdConfigs = [\n                SELECT Id\n                FROM FEC_Hold_Case_Config__c\n                WHERE FEC_Nature_of_Cases__c = :noc.Id\n                LIMIT 1\n            ];\n\n            result.showHoldCase =\n                !holdConfigs.isEmpty();\n\n            return result;\n\n        } catch (Exception ex) {\n\n            throw new AuraHandledException(\n                ex.getMessage()\n            );\n        }\n    }\n}
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/force-app/main/default/classes/FEC_SubProcessService.cls b/force-app/main/default/classes/FEC_SubProcessService.cls
--- a/force-app/main/default/classes/FEC_SubProcessService.cls	(revision 31f8a0255be6d9716986d53078c581c26c26130c)
+++ b/force-app/main/default/classes/FEC_SubProcessService.cls	(date 1778742663023)
@@ -96,16 +96,37 @@
                 noc.FEC_Transfer_Call_to_Collection__c;
 
             // ===== HOLD CASE =====
-
+            // tungnm37: chỉ hiện button Hold Case khi có config Manual
+            // và Case đang ở Stage xử lý của CS Support/CS Customer Care
             List<FEC_Hold_Case_Config__c> holdConfigs = [
-                SELECT Id
+                SELECT Id, Hold_Case_Type__c, FEC_Case_Stage__c
                 FROM FEC_Hold_Case_Config__c
                 WHERE FEC_Nature_of_Cases__c = :noc.Id
-                LIMIT 1
+                  AND Hold_Case_Type__c = 'Manual'
+                  AND FEC_Active__c = true
+                LIMIT 200
             ];
 
-            result.showHoldCase =
-                !holdConfigs.isEmpty();
+            if (!holdConfigs.isEmpty() && recordId != null) {
+                // tungnm37: lấy Current Case Stage để check
+                List<Case> caseList = [
+                    SELECT FEC_Current_Case_Stage__c
+                    FROM Case WHERE Id = :recordId LIMIT 1
+                ];
+                if (!caseList.isEmpty() && caseList[0].FEC_Current_Case_Stage__c != null) {
+                    Id currentStageId = caseList[0].FEC_Current_Case_Stage__c;
+                    for (FEC_Hold_Case_Config__c cfg : holdConfigs) {
+                        // Nếu config không gắn stage cụ thể hoặc khớp stage hiện tại → hiện button
+                        if (cfg.FEC_Case_Stage__c == null || cfg.FEC_Case_Stage__c == currentStageId) {
+                            result.showHoldCase = true;
+                            break;
+                        }
+                    }
+                } else {
+                    // Chưa có stage → hiện button nếu có config Manual
+                    result.showHoldCase = true;
+                }
+            }
 
             return result;
 
Index: force-app/main/default/lwc/fec_holdCaseManual/fec_holdCaseManual.html
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+><template>\n    <lightning-card>\n        <div class=\"hold-case-modal\">\n\n            <!-- Title -->\n            <div class=\"modal-header\">\n                <h2 class=\"modal-title\">{customLabel.HoldCase}</h2>\n            </div>\n            <hr class=\"divider\" />\n\n            <!-- Form Body -->\n            <div class=\"modal-body\">\n\n                <!-- NFU Reason -->\n                <div class=\"slds-grid slds-gutters slds-m-bottom_small\">\n                    <div class=\"slds-col slds-size_4-of-12 label-col  centered-label\">\n                        <label class=\"slds-form-element__label\">\n                           {customLabel.nfuReason}\n                        </label>\n                    </div>\n                    <div class=\"slds-col slds-size_8-of-12\">\n                        <lightning-combobox\n                            name=\"nfuReason\"\n                            value={selectedReason}\n                            placeholder=\"-- Select NFU Reason --\"\n                            options={nfuReasonOptions}\n                            onchange={handleReasonChange}\n                            class=\"reason-combobox\">\n                        </lightning-combobox>\n                    </div>\n                </div>\n\n                <!-- NFU Code -->\n                <div class=\"slds-grid slds-gutters slds-m-bottom_small\">\n                    <div class=\"slds-col slds-size_4-of-12 label-col\">\n                        <label class=\"slds-form-element__label\">\n                            {customLabel.nfuCode}\n                        </label>\n                    </div>\n                    <div class=\"slds-col slds-size_8-of-12 value-col\">\n                        <span class=\"readonly-value\">{nfuCode}</span>\n                    </div>\n                </div>\n\n                <!-- NFU Hold Day Duration -->\n                <div class=\"slds-grid slds-gutters slds-m-bottom_small\">\n                    <div class=\"slds-col slds-size_4-of-12 label-col\">\n                        <label class=\"slds-form-element__label\">\n                           {customLabel.nfuHoldDayDuration}\n                        </label>\n                    </div>\n                    <div class=\"slds-col slds-size_8-of-12 value-col\">\n                        <span class=\"readonly-value\">{nfuHoldDayDuration}</span>\n                    </div>\n                </div>\n\n                <div lwc:if={isHoldCaseSuccess} class=\"slds-grid slds-gutters slds-m-bottom_small\">\n                    <div class=\"slds-col slds-size_4-of-12 label-col\">\n                        <label class=\"slds-text-color_success\">\n                            {customLabel.messengerSuccessHoldCase}\n                        </label>\n                    </div>\n                </div>\n            </div>\n\n            <!-- Footer Buttons -->\n            <div class=\"modal-footer\">\n                <lightning-button\n                    label=\"Cancel\"\n                    onclick={handleCancel}\n                    class=\"cancel-btn\">\n                </lightning-button>\n                <lightning-button\n                    label=\"Hold Case\"\n                    variant=\"brand\"\n                    onclick={handleHoldCase}\n                    disabled={isHoldDisabled}\n                    class=\"hold-btn\">\n                </lightning-button>\n            </div>\n\n        </div>\n    </lightning-card>\n</template>
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/force-app/main/default/lwc/fec_holdCaseManual/fec_holdCaseManual.html b/force-app/main/default/lwc/fec_holdCaseManual/fec_holdCaseManual.html
--- a/force-app/main/default/lwc/fec_holdCaseManual/fec_holdCaseManual.html	(revision 31f8a0255be6d9716986d53078c581c26c26130c)
+++ b/force-app/main/default/lwc/fec_holdCaseManual/fec_holdCaseManual.html	(date 1778744631014)
@@ -1,84 +1,71 @@
 <template>
-    <lightning-card>
-        <div class="hold-case-modal">
+    <div class="hold-case-wrapper">
 
-            <!-- Title -->
-            <div class="modal-header">
-                <h2 class="modal-title">{customLabel.HoldCase}</h2>
-            </div>
-            <hr class="divider" />
+        <!-- Title -->
+        <div class="hold-case-title">
+            <h2>Hold Case</h2>
+        </div>
+        <hr class="hold-case-divider" />
 
-            <!-- Form Body -->
-            <div class="modal-body">
+        <!-- Body -->
+        <div class="hold-case-body">
 
-                <!-- NFU Reason -->
-                <div class="slds-grid slds-gutters slds-m-bottom_small">
-                    <div class="slds-col slds-size_4-of-12 label-col  centered-label">
-                        <label class="slds-form-element__label">
-                           {customLabel.nfuReason}
-                        </label>
-                    </div>
-                    <div class="slds-col slds-size_8-of-12">
-                        <lightning-combobox
-                            name="nfuReason"
-                            value={selectedReason}
-                            placeholder="-- Select NFU Reason --"
-                            options={nfuReasonOptions}
-                            onchange={handleReasonChange}
-                            class="reason-combobox">
-                        </lightning-combobox>
-                    </div>
-                </div>
+            <!-- NFU Reason -->
+            <div class="hold-case-row">
+                <label class="hold-case-label">{customLabel.nfuReason}</label>
+                <div class="hold-case-value">
+                    <lightning-combobox
+                        name="nfuReason"
+                        value={selectedReason}
+                        placeholder="-- Select NFU Reason --"
+                        options={nfuReasonOptions}
+                        onchange={handleReasonChange}>
+                    </lightning-combobox>
+                </div>
+            </div>
 
-                <!-- NFU Code -->
-                <div class="slds-grid slds-gutters slds-m-bottom_small">
-                    <div class="slds-col slds-size_4-of-12 label-col">
-                        <label class="slds-form-element__label">
-                            {customLabel.nfuCode}
-                        </label>
-                    </div>
-                    <div class="slds-col slds-size_8-of-12 value-col">
-                        <span class="readonly-value">{nfuCode}</span>
-                    </div>
-                </div>
+            <!-- NFU Code -->
+            <div class="hold-case-row">
+                <label class="hold-case-label">{customLabel.nfuCode}</label>
+                <div class="hold-case-value">
+                    <span>{nfuCode}</span>
+                </div>
+            </div>
 
-                <!-- NFU Hold Day Duration -->
-                <div class="slds-grid slds-gutters slds-m-bottom_small">
-                    <div class="slds-col slds-size_4-of-12 label-col">
-                        <label class="slds-form-element__label">
-                           {customLabel.nfuHoldDayDuration}
-                        </label>
-                    </div>
-                    <div class="slds-col slds-size_8-of-12 value-col">
-                        <span class="readonly-value">{nfuHoldDayDuration}</span>
-                    </div>
-                </div>
+            <!-- NFU Hold Day Duration -->
+            <div class="hold-case-row">
+                <label class="hold-case-label">{customLabel.nfuHoldDayDuration}</label>
+                <div class="hold-case-value">
+                    <span>{nfuHoldDayDuration}</span>
+                </div>
+            </div>
 
-                <div lwc:if={isHoldCaseSuccess} class="slds-grid slds-gutters slds-m-bottom_small">
-                    <div class="slds-col slds-size_4-of-12 label-col">
-                        <label class="slds-text-color_success">
-                            {customLabel.messengerSuccessHoldCase}
-                        </label>
-                    </div>
+            <!-- Response message - success (xanh lá) -->
+            <template if:true={isSuccessMessage}>
+                <div class="hold-case-message hold-case-message--success">
+                    {responseMessage}
+                </div>
+            </template>
+
+            <!-- Response message - error (đỏ) -->
+            <template if:true={isErrorMessage}>
+                <div class="hold-case-message hold-case-message--error">
+                    {responseMessage}
                 </div>
-            </div>
+            </template>
+
+        </div>
 
-            <!-- Footer Buttons -->
-            <div class="modal-footer">
-                <lightning-button
-                    label="Cancel"
-                    onclick={handleCancel}
-                    class="cancel-btn">
-                </lightning-button>
-                <lightning-button
-                    label="Hold Case"
-                    variant="brand"
-                    onclick={handleHoldCase}
-                    disabled={isHoldDisabled}
-                    class="hold-btn">
-                </lightning-button>
-            </div>
+        <!-- Footer -->
+        <div class="hold-case-footer">
+            <lightning-button label="Cancel" onclick={handleCancel}></lightning-button>
+            <lightning-button
+                variant="brand"
+                label="Hold Case"
+                onclick={handleHoldCase}
+                disabled={isHoldDisabled}>
+            </lightning-button>
+        </div>
 
-        </div>
-    </lightning-card>
-</template>
\ No newline at end of file
+    </div>
+</template>
Index: force-app/main/default/lwc/fec_holdCaseManual/fec_holdCaseManual.js
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>import { LightningElement, api, track, wire } from 'lwc';\nimport { ShowToastEvent } from 'lightning/platformShowToastEvent';\nimport { CloseActionScreenEvent } from 'lightning/actions';\nimport getNFUReasons from '@salesforce/apex/FEC_HoldCaseController.getNFUReasons';\nimport saveHoldCase from '@salesforce/apex/FEC_HoldCaseController.saveHoldCase';\nimport { getRecord, getFieldValue }  from 'lightning/uiRecordApi';\n\n// ── Case Field Imports ────────────────────────────────────────────────────────\nimport FIELD_ID              from '@salesforce/schema/Case.Id';\nimport FIELD_PRODUCT_TYPE    from '@salesforce/schema/Case.FEC_Product_Type__c';\nimport FIELD_CATEGORY        from '@salesforce/schema/Case.FEC_Category__c';\nimport FIELD_SUB_CATEGORY    from '@salesforce/schema/Case.FEC_SubCategory__c';\nimport FIELD_SUB_CODE        from '@salesforce/schema/Case.FEC_SubCode__c';\nimport FIELD_IS_SUBMITTED    from '@salesforce/schema/Case.FEC_Is_Submited__c';\nimport FEC_NFU_Reason from '@salesforce/label/c.FEC_NFU_Reason';\nimport FEC_Hold_Case from '@salesforce/label/c.FEC_Hold_Case';\nimport FEC_NFU_Code from '@salesforce/label/c.FEC_NFU_Code';\nimport FEC_NFU_Hold_Day_Duration from '@salesforce/label/c.FEC_NFU_Hold_Day_Duration';\nimport FEC_Messenger_Success_Hold_Case from '@salesforce/label/c.FEC_Messenger_Success_Hold_Case';\nimport FEC_Error_MessageError_Hold_Case from '@salesforce/label/c.FEC_Error_MessageError_Hold_Case';\nimport FEC_Error_Message_Maxretris from '@salesforce/label/c.FEC_Error_Message_Maxretris';\nimport FEC_Show_Action_Hold_Case from '@salesforce/label/c.FEC_Show_Action_Hold_Case'; \nimport FEC_Failed_To_Load_Case from '@salesforce/label/c.FEC_Failed_To_Load_Case'; \nimport FEC_Failed_To_Load_NFU from '@salesforce/label/c.FEC_Failed_To_Load_NFU'; \nimport FEC_Please_Select_An_NFU from '@salesforce/label/c.FEC_Please_Select_An_NFU'; \nimport FEC_MSG_IPP_AddIpp_Default_Success from '@salesforce/label/c.FEC_MSG_IPP_AddIpp_Default_Success'; \nimport FEC_An_Unexpected_Error from '@salesforce/label/c.FEC_An_Unexpected_Error'; \nimport {\n  STR_EMPTY, ERROR_TOAST_TYPE, WARNING_HOLD_CASE,\n   WARNING_HOLD_TOAST, SUCCESS_MODAL_TITLE,\n    RESPONSE_SUCCESS,ERROR_MODAL_TITLE\n} from \"c/fec_CommonConst\";\n\n\n// ── All fields array for getRecord ───────────────────────────────────────────\nconst CASE_FIELDS = [\n    FIELD_ID,\n    FIELD_PRODUCT_TYPE,\n    FIELD_CATEGORY,\n    FIELD_SUB_CATEGORY,\n    FIELD_SUB_CODE,\n    FIELD_IS_SUBMITTED\n];\n\nexport default class Fec_holdCaseManual extends LightningElement {\n\n    // ── Public Properties ────────────────────────────────────────────────────\n    @api recordId; // Case Id from record page\n\n    // ── Tracked State ────────────────────────────────────────────────────────\n    @track selectedReason = STR_EMPTY;\n    @track nfuCode = STR_EMPTY;\n    @track nfuHoldDayDuration = STR_EMPTY;\n    @track nfuReasonOptions = [];\n    @track isLoading = false;\n\n    \n    // ── Case Record Fields ───────────────────────────────────────────────────\n    @track caseNumber         = STR_EMPTY;\n    @track caseAccount        = STR_EMPTY;\n    @track caseProductType    = STR_EMPTY;\n    @track caseCategory       = STR_EMPTY;\n    @track caseSubCategory    = STR_EMPTY;\n    @track caseSubCode        = STR_EMPTY;\n    @track caseIsSubmitted    = false;\n    @track isHoldCaseSuccess  = false;\n    @track errorMessage       = STR_EMPTY;\n\n    \n    // ── Internal Map: reason value → { code, duration } ─────────────────────\n    _nfuReasonMap = {}; \n\n    _retryCount = 0;\n    _maxRetries = 3;\n\n     customLabel = {\n       nfuReason : FEC_NFU_Reason,\n       HoldCase : FEC_Hold_Case,\n       nfuCode : FEC_NFU_Code,\n       nfuHoldDayDuration: FEC_NFU_Hold_Day_Duration,\n       messengerSuccessHoldCase : FEC_Messenger_Success_Hold_Case,\n       errorMessageErrorHoldCase : FEC_Error_MessageError_Hold_Case,\n       errorMessageMaxretris : FEC_Error_Message_Maxretris,\n       showActionHoldCase : FEC_Show_Action_Hold_Case,\n       failedToLoadCase : FEC_Failed_To_Load_Case,\n       failedToLoadNFU : FEC_Failed_To_Load_NFU,\n       pleaseSelectAnNFU : FEC_Please_Select_An_NFU,\n       mSGIPPAddIppDefaultSuccess : FEC_MSG_IPP_AddIpp_Default_Success,\n       anUnexpectedError : FEC_An_Unexpected_Error\n    }\n\n    // ─────────────────────────────────────────────────────────────────────────\n    // WIRE : getRecord\n    // DESC : Fetch Case record fields reactively by recordId\n    // ─────────────────────────────────────────────────────────────────────────\n    @wire(getRecord, { recordId: '$recordId', fields: CASE_FIELDS })\n    wiredCase({ error, data }) {\n        if (data) {\n            this.caseSubCode     = getFieldValue(data, FIELD_SUB_CODE);\n            console.log('SubCode:', this.caseSubCode);\n            this.caseIsSubmitted = getFieldValue(data, FIELD_IS_SUBMITTED);\n            if (this.caseSubCode) {\n                this._loadNFUReasons();\n            } else {\n                this.handleCancel();\n                this._showToastNotFoundCaseConfig();\n            }\n\n        } else if (error) {\n            this._showToast(\n                STR_EMPTY,\n                this.customLabel.failedToLoadCase + (error?.body?.message ?? error),\n                ERROR_TOAST_TYPE\n            );\n        }\n    }\n    \n    // ─────────────────────────────────────────────────────────────────────────\n    // PRIVATE : _loadNFUReasons\n    // DESC    : Async/await call to getNFUReasons Apex method\n    //           Guarded by subCode blank check before invoking\n    // ─────────────────────────────────────────────────────────────────────────\n    async _loadNFUReasons() {\n        // ── Guard: skip if subCode is blank ───────────────────────────────────\n        if (!this.caseSubCode) {\n            return;\n        }\n\n        this.isLoading = true;\n        try {\n            // ── Call Apex getNFUReasons with subCode ───────────────────────────\n            const data = await getNFUReasons({ subCode: this.caseSubCode });\n            if (data && data?.length > 0) {\n                // ── Build combobox options — active configs only ───────────────────\n                this.nfuReasonOptions = data\n                    .filter(item => item.isActive)\n                    .map(item => ({\n                        label : item.nfuReasonVN || item.nfuReasonEN,\n                        value : item.nfuCode\n                    }));\n    \n                // ── Build internal map for auto-populate on selection ─────────────\n                this._nfuReasonMap = {};\n                data.forEach(item => {\n                    this._nfuReasonMap[item.nfuCode] = {\n                        nfuDuration  : item.nfuDuration,\n                        holdCaseType : item.holdCaseType,\n                        nfuReasonVN  : item.nfuReasonVN,\n                        nfuReasonEN  : item.nfuReasonEN,\n                        nfuCode      : item.nfuCode,\n                        subCode      : item.subCode\n                    };\n                });\n            } else {\n                this.handleCancel();\n                this._showToastNotFoundCaseConfig();\n            }\n\n        } catch (error) {\n            this._showToast(\n                STR_EMPTY,\n                this.customLabel.failedToLoadNFU + (error?.body?.message ?? error),\n                ERROR_TOAST_TYPE\n            );\n        } finally {\n            this.isLoading = false;\n        }\n    }\n\n    // ── Computed ─────────────────────────────────────────────────────────────\n    get isHoldDisabled() {\n        return !this.selectedReason;\n    }\n\n    // ── Handlers ─────────────────────────────────────────────────────────────\n\n    /**\n     * On NFU Reason change → auto-populate NFU Code & Hold Day Duration\n     */\n    handleReasonChange(event) {\n        this.selectedReason = event.detail.value;\n\n        const meta = this._nfuReasonMap[this.selectedReason];\n        if (meta) {\n            this.nfuCode = meta.nfuCode;\n            this.nfuHoldDayDuration = meta.nfuDuration;\n        } else {\n            this.nfuCode = STR_EMPTY;\n            this.nfuHoldDayDuration = STR_EMPTY;\n        }\n    }\n\n    /**\n     * Cancel → close the action screen\n     */\n    handleCancel() {\n        this.dispatchEvent(new CloseActionScreenEvent());\n    }\n\n    /**\n     * Hold Case → call Apex to update Case status\n     */\n    async handleHoldCase() {\n        if (!this.selectedReason) {\n            this._showToast(WARNING_HOLD_CASE, this.customLabel.pleaseSelectAnNFU, WARNING_HOLD_TOAST);\n            return;\n        }\n        this.isLoading = true;\n        try {\n            const response = await saveHoldCase({\n                caseId       : this.recordId,\n                nfuReason    : this.selectedReason,\n                nfuCode      : this.nfuCode,\n                holdDuration : this.nfuHoldDayDuration\n            });\n            this._retryCount += 1;\n\n            if (response?.status == RESPONSE_SUCCESS) {\n                this.isHoldCaseSuccess = true;\n                this._showToast(this.customLabel.mSGIPPAddIppDefaultSuccess, this.customLabel.messengerSuccessHoldCase, SUCCESS_MODAL_TITLE);\n                this.handleCancel();\n            } else {\n                \n                // update retry count and show error if max retries reached\n                if (this._retryCount >= this._maxRetries) {\n                    this.errorMessage = this.customLabel.errorMessageErrorHoldCase;\n                    // update logic\n                } else {\n                    this.errorMessage = this.customLabel.errorMessageMaxretris;\n                    // update logic\n                }\n            }\n           \n\n        } catch (error) {\n            const msg = error?.body?.message ?? this.customLabel.anUnexpectedError;\n            this._showToast(ERROR_MODAL_TITLE, msg, ERROR_TOAST_TYPE);\n        } finally {\n            this.isLoading = false;\n        }\n    }\n\n    // ── Private Helpers ──────────────────────────────────────────────────────\n    _showToast(title, message, variant) {\n        this.dispatchEvent(new ShowToastEvent({ title, message, variant }));\n    }\n\n    _showToastNotFoundCaseConfig() {\n        this._showToast( STR_EMPTY, this.customLabel.showActionHoldCase, ERROR_TOAST_TYPE);\n    }\n}
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/force-app/main/default/lwc/fec_holdCaseManual/fec_holdCaseManual.js b/force-app/main/default/lwc/fec_holdCaseManual/fec_holdCaseManual.js
--- a/force-app/main/default/lwc/fec_holdCaseManual/fec_holdCaseManual.js	(revision 31f8a0255be6d9716986d53078c581c26c26130c)
+++ b/force-app/main/default/lwc/fec_holdCaseManual/fec_holdCaseManual.js	(date 1778744433280)
@@ -3,15 +3,7 @@
 import { CloseActionScreenEvent } from 'lightning/actions';
 import getNFUReasons from '@salesforce/apex/FEC_HoldCaseController.getNFUReasons';
 import saveHoldCase from '@salesforce/apex/FEC_HoldCaseController.saveHoldCase';
-import { getRecord, getFieldValue }  from 'lightning/uiRecordApi';
 
-// ── Case Field Imports ────────────────────────────────────────────────────────
-import FIELD_ID              from '@salesforce/schema/Case.Id';
-import FIELD_PRODUCT_TYPE    from '@salesforce/schema/Case.FEC_Product_Type__c';
-import FIELD_CATEGORY        from '@salesforce/schema/Case.FEC_Category__c';
-import FIELD_SUB_CATEGORY    from '@salesforce/schema/Case.FEC_SubCategory__c';
-import FIELD_SUB_CODE        from '@salesforce/schema/Case.FEC_SubCode__c';
-import FIELD_IS_SUBMITTED    from '@salesforce/schema/Case.FEC_Is_Submited__c';
 import FEC_NFU_Reason from '@salesforce/label/c.FEC_NFU_Reason';
 import FEC_Hold_Case from '@salesforce/label/c.FEC_Hold_Case';
 import FEC_NFU_Code from '@salesforce/label/c.FEC_NFU_Code';
@@ -19,167 +11,111 @@
 import FEC_Messenger_Success_Hold_Case from '@salesforce/label/c.FEC_Messenger_Success_Hold_Case';
 import FEC_Error_MessageError_Hold_Case from '@salesforce/label/c.FEC_Error_MessageError_Hold_Case';
 import FEC_Error_Message_Maxretris from '@salesforce/label/c.FEC_Error_Message_Maxretris';
-import FEC_Show_Action_Hold_Case from '@salesforce/label/c.FEC_Show_Action_Hold_Case'; 
-import FEC_Failed_To_Load_Case from '@salesforce/label/c.FEC_Failed_To_Load_Case'; 
-import FEC_Failed_To_Load_NFU from '@salesforce/label/c.FEC_Failed_To_Load_NFU'; 
-import FEC_Please_Select_An_NFU from '@salesforce/label/c.FEC_Please_Select_An_NFU'; 
-import FEC_MSG_IPP_AddIpp_Default_Success from '@salesforce/label/c.FEC_MSG_IPP_AddIpp_Default_Success'; 
-import FEC_An_Unexpected_Error from '@salesforce/label/c.FEC_An_Unexpected_Error'; 
+import FEC_Show_Action_Hold_Case from '@salesforce/label/c.FEC_Show_Action_Hold_Case';
+import FEC_Failed_To_Load_NFU from '@salesforce/label/c.FEC_Failed_To_Load_NFU';
+import FEC_Please_Select_An_NFU from '@salesforce/label/c.FEC_Please_Select_An_NFU';
+import FEC_An_Unexpected_Error from '@salesforce/label/c.FEC_An_Unexpected_Error';
 import {
-  STR_EMPTY, ERROR_TOAST_TYPE, WARNING_HOLD_CASE,
-   WARNING_HOLD_TOAST, SUCCESS_MODAL_TITLE,
-    RESPONSE_SUCCESS,ERROR_MODAL_TITLE
-} from "c/fec_CommonConst";
-
-
-// ── All fields array for getRecord ───────────────────────────────────────────
-const CASE_FIELDS = [
-    FIELD_ID,
-    FIELD_PRODUCT_TYPE,
-    FIELD_CATEGORY,
-    FIELD_SUB_CATEGORY,
-    FIELD_SUB_CODE,
-    FIELD_IS_SUBMITTED
-];
+    STR_EMPTY, ERROR_TOAST_TYPE, WARNING_HOLD_CASE,
+    WARNING_HOLD_TOAST, SUCCESS_MODAL_TITLE, ERROR_MODAL_TITLE
+} from 'c/fec_CommonConst';
 
 export default class Fec_holdCaseManual extends LightningElement {
 
-    // ── Public Properties ────────────────────────────────────────────────────
-    @api recordId; // Case Id from record page
+    @api
+    get recordId() { return this._recordId; }
+    set recordId(val) {
+        this._recordId = val;
+        if (val) this._loadNFUReasons();
+    }
+    _recordId;
 
-    // ── Tracked State ────────────────────────────────────────────────────────
     @track selectedReason = STR_EMPTY;
     @track nfuCode = STR_EMPTY;
     @track nfuHoldDayDuration = STR_EMPTY;
     @track nfuReasonOptions = [];
     @track isLoading = false;
-
-    
-    // ── Case Record Fields ───────────────────────────────────────────────────
-    @track caseNumber         = STR_EMPTY;
-    @track caseAccount        = STR_EMPTY;
-    @track caseProductType    = STR_EMPTY;
-    @track caseCategory       = STR_EMPTY;
-    @track caseSubCategory    = STR_EMPTY;
-    @track caseSubCode        = STR_EMPTY;
-    @track caseIsSubmitted    = false;
-    @track isHoldCaseSuccess  = false;
-    @track errorMessage       = STR_EMPTY;
+    @track errorMessage = STR_EMPTY;
+    // tungnm37: TH1 = already marked (xanh lá), TH2 = success (xanh lá), TH3 = max retries (đỏ)
+    @track responseMessage = STR_EMPTY;
+    @track responseType = STR_EMPTY; // 'ALREADY_MARKED' | 'SUCCESS' | 'ERROR'
+    // tungnm37: NFU info sau khi gọi API thành công
+    @track nfuStatus = STR_EMPTY;
+    @track nfuStartedDate = STR_EMPTY;
+    @track nfuExpiryDate = STR_EMPTY;
+    @track nfuReason = STR_EMPTY;
 
-    
-    // ── Internal Map: reason value → { code, duration } ─────────────────────
-    _nfuReasonMap = {}; 
-
+    _nfuReasonMap = {};
     _retryCount = 0;
     _maxRetries = 3;
 
-     customLabel = {
-       nfuReason : FEC_NFU_Reason,
-       HoldCase : FEC_Hold_Case,
-       nfuCode : FEC_NFU_Code,
-       nfuHoldDayDuration: FEC_NFU_Hold_Day_Duration,
-       messengerSuccessHoldCase : FEC_Messenger_Success_Hold_Case,
-       errorMessageErrorHoldCase : FEC_Error_MessageError_Hold_Case,
-       errorMessageMaxretris : FEC_Error_Message_Maxretris,
-       showActionHoldCase : FEC_Show_Action_Hold_Case,
-       failedToLoadCase : FEC_Failed_To_Load_Case,
-       failedToLoadNFU : FEC_Failed_To_Load_NFU,
-       pleaseSelectAnNFU : FEC_Please_Select_An_NFU,
-       mSGIPPAddIppDefaultSuccess : FEC_MSG_IPP_AddIpp_Default_Success,
-       anUnexpectedError : FEC_An_Unexpected_Error
-    }
-
-    // ─────────────────────────────────────────────────────────────────────────
-    // WIRE : getRecord
-    // DESC : Fetch Case record fields reactively by recordId
-    // ─────────────────────────────────────────────────────────────────────────
-    @wire(getRecord, { recordId: '$recordId', fields: CASE_FIELDS })
-    wiredCase({ error, data }) {
-        if (data) {
-            this.caseSubCode     = getFieldValue(data, FIELD_SUB_CODE);
-            console.log('SubCode:', this.caseSubCode);
-            this.caseIsSubmitted = getFieldValue(data, FIELD_IS_SUBMITTED);
-            if (this.caseSubCode) {
-                this._loadNFUReasons();
-            } else {
-                this.handleCancel();
-                this._showToastNotFoundCaseConfig();
-            }
+    customLabel = {
+        nfuReason: FEC_NFU_Reason,
+        HoldCase: FEC_Hold_Case,
+        nfuCode: FEC_NFU_Code,
+        nfuHoldDayDuration: FEC_NFU_Hold_Day_Duration,
+        messengerSuccessHoldCase: FEC_Messenger_Success_Hold_Case,
+        errorMessageErrorHoldCase: FEC_Error_MessageError_Hold_Case,
+        errorMessageMaxretris: FEC_Error_Message_Maxretris,
+        showActionHoldCase: FEC_Show_Action_Hold_Case,
+        failedToLoadNFU: FEC_Failed_To_Load_NFU,
+        pleaseSelectAnNFU: FEC_Please_Select_An_NFU,
+        anUnexpectedError: FEC_An_Unexpected_Error
+    };
 
-        } else if (error) {
-            this._showToast(
-                STR_EMPTY,
-                this.customLabel.failedToLoadCase + (error?.body?.message ?? error),
-                ERROR_TOAST_TYPE
-            );
-        }
+    connectedCallback() {
+        // recordId setter sẽ gọi _loadNFUReasons khi được set
     }
-    
-    // ─────────────────────────────────────────────────────────────────────────
-    // PRIVATE : _loadNFUReasons
-    // DESC    : Async/await call to getNFUReasons Apex method
-    //           Guarded by subCode blank check before invoking
-    // ─────────────────────────────────────────────────────────────────────────
+
+    // tungnm37: load NFU Reasons theo caseId (query NOC từ Case)
     async _loadNFUReasons() {
-        // ── Guard: skip if subCode is blank ───────────────────────────────────
-        if (!this.caseSubCode) {
-            return;
-        }
-
         this.isLoading = true;
         try {
-            // ── Call Apex getNFUReasons with subCode ───────────────────────────
-            const data = await getNFUReasons({ subCode: this.caseSubCode });
-            if (data && data?.length > 0) {
-                // ── Build combobox options — active configs only ───────────────────
+            const data = await getNFUReasons({ caseId: this._recordId });
+            if (data && data.length > 0) {
                 this.nfuReasonOptions = data
                     .filter(item => item.isActive)
                     .map(item => ({
-                        label : item.nfuReasonVN || item.nfuReasonEN,
-                        value : item.nfuCode
+                        label: item.nfuReasonVN || item.nfuReasonEN,
+                        value: item.nfuCode
                     }));
-    
-                // ── Build internal map for auto-populate on selection ─────────────
+
                 this._nfuReasonMap = {};
                 data.forEach(item => {
                     this._nfuReasonMap[item.nfuCode] = {
-                        nfuDuration  : item.nfuDuration,
-                        holdCaseType : item.holdCaseType,
-                        nfuReasonVN  : item.nfuReasonVN,
-                        nfuReasonEN  : item.nfuReasonEN,
-                        nfuCode      : item.nfuCode,
-                        subCode      : item.subCode
+                        nfuDuration: item.nfuDuration,
+                        holdCaseType: item.holdCaseType,
+                        nfuReasonVN: item.nfuReasonVN,
+                        nfuCode: item.nfuCode
                     };
                 });
             } else {
                 this.handleCancel();
-                this._showToastNotFoundCaseConfig();
+                this._showToast(STR_EMPTY, this.customLabel.showActionHoldCase, ERROR_TOAST_TYPE);
             }
-
         } catch (error) {
-            this._showToast(
-                STR_EMPTY,
-                this.customLabel.failedToLoadNFU + (error?.body?.message ?? error),
-                ERROR_TOAST_TYPE
-            );
+            this._showToast(STR_EMPTY, this.customLabel.failedToLoadNFU + (error?.body?.message ?? error), ERROR_TOAST_TYPE);
         } finally {
             this.isLoading = false;
         }
     }
 
-    // ── Computed ─────────────────────────────────────────────────────────────
     get isHoldDisabled() {
-        return !this.selectedReason;
+        return !this.selectedReason || this._retryCount >= this._maxRetries;
     }
 
-    // ── Handlers ─────────────────────────────────────────────────────────────
+    // tungnm37: hiện message xanh lá khi TH1 hoặc TH2
+    get isSuccessMessage() {
+        return this.responseType === 'SUCCESS' || this.responseType === 'ALREADY_MARKED';
+    }
 
-    /**
-     * On NFU Reason change → auto-populate NFU Code & Hold Day Duration
-     */
+    // tungnm37: hiện message đỏ khi lỗi
+    get isErrorMessage() {
+        return this.responseType === 'ERROR';
+    }
+
     handleReasonChange(event) {
         this.selectedReason = event.detail.value;
-
         const meta = this._nfuReasonMap[this.selectedReason];
         if (meta) {
             this.nfuCode = meta.nfuCode;
@@ -188,64 +124,75 @@
             this.nfuCode = STR_EMPTY;
             this.nfuHoldDayDuration = STR_EMPTY;
         }
+        // Reset message khi chọn lại
+        this.responseMessage = STR_EMPTY;
+        this.responseType = STR_EMPTY;
     }
 
-    /**
-     * Cancel → close the action screen
-     */
     handleCancel() {
         this.dispatchEvent(new CloseActionScreenEvent());
     }
 
-    /**
-     * Hold Case → call Apex to update Case status
-     */
     async handleHoldCase() {
         if (!this.selectedReason) {
             this._showToast(WARNING_HOLD_CASE, this.customLabel.pleaseSelectAnNFU, WARNING_HOLD_TOAST);
             return;
         }
+        if (this._retryCount >= this._maxRetries) return;
+
         this.isLoading = true;
         try {
             const response = await saveHoldCase({
-                caseId       : this.recordId,
-                nfuReason    : this.selectedReason,
-                nfuCode      : this.nfuCode,
-                holdDuration : this.nfuHoldDayDuration
+                caseId: this._recordId,
+                nfuReason: this.selectedReason,
+                nfuCode: this.nfuCode,
+                holdDuration: this.nfuHoldDayDuration
             });
-            this._retryCount += 1;
-
-            if (response?.status == RESPONSE_SUCCESS) {
-                this.isHoldCaseSuccess = true;
-                this._showToast(this.customLabel.mSGIPPAddIppDefaultSuccess, this.customLabel.messengerSuccessHoldCase, SUCCESS_MODAL_TITLE);
-                this.handleCancel();
-            } else {
-                
-                // update retry count and show error if max retries reached
+
+            if (response?.responseType === 'SUCCESS') {
+                // tungnm37: TH2 - Success
+                this.responseType = 'SUCCESS';
+                this.responseMessage = this.customLabel.messengerSuccessHoldCase;
+                // Hiển thị NFU info
+                this.nfuCode = response.nfuCode || this.nfuCode;
+                this.nfuStatus = response.nfuStatus || STR_EMPTY;
+                this.nfuExpiryDate = response.nfuExpiryDate || STR_EMPTY;
+                this.nfuReason = response.nfuReason || this.selectedReason;
+                this._retryCount = 0;
+
+            } else if (response?.responseType === 'ALREADY_MARKED') {
+                // tungnm37: TH1 - Contract has already marked as NFU
+                this.responseType = 'ALREADY_MARKED';
+                this.responseMessage = response.message || STR_EMPTY;
+                this._retryCount = 0;
+
+            } else {
+                // tungnm37: Error - tăng retry count
+                this._retryCount += 1;
+                this.responseType = 'ERROR';
                 if (this._retryCount >= this._maxRetries) {
-                    this.errorMessage = this.customLabel.errorMessageErrorHoldCase;
-                    // update logic
+                    // TH3: max retries → hiện Noti-02 khác
+                    this.responseMessage = this.customLabel.errorMessageErrorHoldCase;
                 } else {
-                    this.errorMessage = this.customLabel.errorMessageMaxretris;
-                    // update logic
+                    this.responseMessage = this.customLabel.errorMessageMaxretris;
                 }
             }
-           
 
         } catch (error) {
+            this._retryCount += 1;
+            this.responseType = 'ERROR';
             const msg = error?.body?.message ?? this.customLabel.anUnexpectedError;
-            this._showToast(ERROR_MODAL_TITLE, msg, ERROR_TOAST_TYPE);
+            if (this._retryCount >= this._maxRetries) {
+                this.responseMessage = this.customLabel.errorMessageErrorHoldCase;
+            } else {
+                this.responseMessage = msg;
+            }
         } finally {
             this.isLoading = false;
         }
     }
 
-    // ── Private Helpers ──────────────────────────────────────────────────────
     _showToast(title, message, variant) {
         this.dispatchEvent(new ShowToastEvent({ title, message, variant }));
     }
-
-    _showToastNotFoundCaseConfig() {
-        this._showToast( STR_EMPTY, this.customLabel.showActionHoldCase, ERROR_TOAST_TYPE);
-    }
 }
\ No newline at end of file
